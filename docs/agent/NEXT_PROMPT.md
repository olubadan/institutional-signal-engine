# Proposed Next Codex Prompt

Independently validate the exact-head Phase 4B evidence-bundle correction on
draft PR #6. Confirm `J = seal(compose(E_det, Sigma))`, `P = persist(J)`,
`R = replay(P)`, `Omega = (J, P, R)`, and `Certificate = pi_CDelta(Omega)`;
reconstruct only from P's content-addressed object, rerun production structural
and closed semantic verification, and confirm the published J/P/R/certificate
hashes and exact-head CI. Do not modify PR #5, mark PR #6 ready, run providers,
enable trading, create orders, merge, or begin Phase 5.
