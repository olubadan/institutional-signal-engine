# SESSION-0027 Prompt

Perform one bounded final causal-journal correction on
`feat/rth-orchestration-recovery`, stacked as draft PR #6 on draft PR #5,
starting exactly at `8820b452a0c429aa41c012df0fa1fbed50830178`.

Trace and repair only the assembled execution's independent clock/event driver,
append-only causal journal, sealing, recalculated structural integrity, semantic
causality, pure single-input acceptance projection, exact persisted replay, and
genuine corruption tests. The runtime journal `J = compose(E_det, Sigma)` must be
the certificate's sole runtime evidence source. External Git, worktree, PR, and
CI facts remain separate build-envelope evidence.

The deterministic composition must prove E1 `{A}`, E2 `{A,B}`, E3 `{B}` with
paired TRADE/QUOTE command acknowledgements before activation, independently
clock-driven reevaluations, accepted/rejected events, disconnect/reconnect and
restoration, stop/drain/finalization, sealed canonical persistence, reconstructed
replay, and exact replayed projection equality. Trading must remain disabled and
orders constructed/submitted must remain `0/0`.

Replace synthetic negatives with mutations of the real sealed journal or its
persisted serialization, each failing through production deserialization,
integrity, semantic-validation, and projection functions with a specific failure
code. Cover all corruptions enumerated by the owner, including record deletion,
reordering, digest/chain/parent/run/command/ack/epoch/event/restoration/clock/
lifecycle/seal/replay failures and internally rehashed fabrications.

Do not change models, thresholds, `CONTROL_V1`, `SHADOW_IMPACT_V1`, the approved
continuous-RTH design, PR #5, provider execution, trading, orders, or Phase 5.
Remove obsolete competing evidence paths. Run the complete requested local and
exact-head verification, produce deterministic journal and certificate artifacts
with SHA-256 digests, update only affected PR #6 evidence, commit and push one
coherent correction, keep PR #6 draft and unmerged, and report the exact final
envelope. If the architecture cannot satisfy the contract coherently, stop and
name the exact obstruction without approximation.
