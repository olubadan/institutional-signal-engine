# SESSION-0029 Prompt

Correct the final repository-authority defect on draft PR #6, starting exactly
at independently rejected head
`e7026dcb803277ec56359ca4ed7d0a8ad9ddcb76`, while leaving draft PR #5
unchanged at `4a3afe8dd70fecc1b629ff7ef5614a652a572529`.

Implement the minimum bounded authoritative lifecycle:

`J = seal(compose(E_det, Sigma))`

`P = persist(J)`

`R = replay(P)`

`Omega_verified = verify_repository_backed(CDelta, J, P, R, repository)`

`Certificate = pi_CDelta(Omega_verified)`

The production verifier must receive the repository used for persistence, load
P's exact content-addressed object, compare its canonical bytes with sealed J,
deserialize a distinct journal, independently verify all structural, semantic,
binding, count, root, digest, and observed-projection facts, derive replay facts
from that reconstruction, and treat caller-supplied R only as a claim to compare.
Only this verifier may issue the verified package accepted by certificate
projection.

Add production-path adversarial tests for absent, wrong, byte-different,
cross-run, and cross-contract repository objects; fabricated or unverified
packages; unreconstructed replay claims; and internally consistent but
repository-unbacked receipts. Preserve the deterministic three-epoch trace and
all existing semantics. Keep P and R outside J and build-envelope evidence
outside the runtime certificate.

Do not change strategy, mathematical model, thresholds, `CONTROL_V1`,
`SHADOW_IMPACT_V1`, unrelated orchestration, providers, PR #5, trading, orders,
PR readiness or merge state, or Phase 5. Do not run providers.

Run Ruff format and lint, strict mypy, dependency-lock verification, the complete
hermetic suite, focused certification tests, structural checks, shell checks,
secret checks, and repeated certification with byte-identical J, P, R, and
certificate artifacts. Publish one coherent correction commit to draft PR #6,
wait for exact-head push and PR CI, and update only affected current evidence.
Do not claim independent acceptance.
