# Prompt Record — SESSION-0032

## Timestamp

`20260811T094647Z`

## Owner instruction

Complete the live observational-session production evidence path on draft PR
#6, beginning exactly at independently accepted head
`3caa6dfffe3b6b08ffdb89aeae2d0ed46e61a294`. Keep PR #5 unchanged, open,
and draft. Do not contact providers, access production credentials, merge,
begin Phase 5, enable trading, or construct or submit orders.

Preserve the accepted hermetic Phase 4B certification subsystem without
changing its mathematical model, authority boundary, or certificate meaning.
Add a separate live observational-session certificate that accepts only one
finalized evidence-bundle path and fresh output destinations, derives semantic
claims from that bundle and pinned checked-in rules, and never claims that the
deterministic acceptance scenario ran.

Build one production composition and independently clocked 09:30–16:00
America/New_York session loop for discovery, enrichment, dynamic paired
TRADE/QUOTE subscription commands, receive-loop acknowledgements, event
processing, scheduled universe reevaluation, admission/removal, disconnect,
reconnect, acknowledgement-gated restoration, intake stop, deterministic drain,
single finalization, terminal journal sealing, durable persistence, offline
replay, equality comparison, and live-session certification. Fail closed for
every acknowledgement, receive-authority, transition, recovery, provider
termination, persistence, identity, corruption, mixed-artifact, shutdown,
trading, and order-activity violation specified by the owner.

Require a fresh explicit output directory and reachable durable persistence
before provider connection. Continuously preserve recoverable incomplete
evidence, publish a completed manifest only after durable finalization, and
publish the live certificate last. Bind the exact commit, application/schema
versions, checked-in resource/configuration hashes, session/market/clock/provider
identities, connection history, discovery/enrichment, correlated command and
acknowledgement history, events, epochs, shutdown, journal count/root/seal,
persistence/replay receipts, equality, safety state, and impact fallback.

Replace stale or ambiguous runbook commands with one exact full-session command
and checked-in environment, health, termination, inspection, replay, and
certification commands. Add CLI-help/runbook regression coverage.

Without providers, exercise the same production composition and session loop
through a virtual-clock 09:30–16:00 session containing discovery,
acknowledgements, events, reevaluation, admission, removal, disconnect,
reconnect, restoration acknowledgements, intake stop, drain, finalization,
persistence, replay, and certification. Add the complete adversarial evidence-
class suite requested by the owner and independently verify the bundle.

Run Ruff format and lint, strict mypy, lock verification, the complete hermetic
and focused live-session suites, structural/Bash/ShellCheck/secret checks, two
accelerated full-session executions, and required byte/semantic equality
checks. Commit the coherent implementation/test/affected operational-doc set,
push only PR #6's branch, wait for exact-head CI, and confirm PR #6 remains open,
draft, mergeable, and unmerged while PR #5 is unchanged. The only successful
completion statement is:

`LIVE PATH BUILD COMPLETE — AWAITING INDEPENDENT VALIDATION`

If any code or operational-path condition remains unresolved, finish with:

`LIVE PATH BUILD BLOCKED`

and the exact remaining condition.
