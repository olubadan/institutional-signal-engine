# Prompt Record — SESSION-0031

## Timestamp

`20260811T083324Z`

## Owner instruction

Complete certification-authority closure on draft PR #6, starting exactly at
independently rejected head `cd4b5890894e97dc3cd062c91844859e8bcd1efd`,
while PR #5 remains unchanged at
`4a3afe8dd70fecc1b629ff7ef5614a652a572529`.

Produce one authoritative Phase 4B certification operation whose only public
semantic input is internally owned and whose preferred interface is
`generate_phase4b_certification(output_paths_only) -> CertificationArtifacts`.
The operation must internally load and digest-pin the package-relative contract,
construct the checked-in three-epoch scenario and owned repository, execute and
seal J, persist and reload P, independently reconstruct and verify R, immediately
project the certificate from those facts, and publish J/P/R/certificate only
after successful authoritative completion.

Before editing, freeze a complete authority map and attack matrix covering every
certificate field and every construction/consumer path. Remove or explicitly
make non-authoritative every alternate certificate, PASS, projection, receipt,
contract, repository, callback, environment/path, CLI, writer, public export,
private semantic-helper, deserialization, and test-import path. Keep certificate
schema validation shape-only and non-authoritative. Bind the contract and
scenario identities, run/journal/persistence/replay/projection facts, schema
version, and supported canonical certificate digest.

Generate systematic adversarial tests from the authority map, including all
specified contract, execution, persistence, replay, certificate, construction,
serialization, cross-run/cross-contract, self-consistent-rehash, and safety
attacks. Require stable failure codes and exercise the actual production
boundary or prove no alternate boundary exists.

After ordinary verification, stop adding code and perform fresh construction-
graph/attack enumerations until two consecutive red-team passes find no new
authoritative bypass. Record the final authority map, matrix, and falsification
results in Phase 4B evidence.

Preserve strategy, model, thresholds, `CONTROL_V1`, `SHADOW_IMPACT_V1`, the
deterministic `{A} -> {A,B} -> {B}` scenario, paired acknowledgements,
acknowledgement-gated activation, clock reevaluation, removed-contract rejection,
restoration, shutdown/finalization, disabled trading, orders `0/0`, PR #5, draft
and unmerged PR #6, and the Phase 5 boundary. Do not run live providers.

Run Ruff format/lint, strict mypy, lock verification, the full hermetic and
focused suites, structural/Bash/ShellCheck/secret/diff checks, contract digest
verification, public API/AST inspection, and two independent full certification
executions with byte-for-byte J/P/R/certificate comparison. Then create one
coherent commit, push exact head to PR #6, wait for exact-head CI, and update the
PR description with the required authority, digest, test, attack, artifact, CI,
PR, and safety evidence. Do not claim independent acceptance.

Finish exactly:

`BUILD COMPLETE — COMPLETE AUTHORITY GRAPH AWAITING INDEPENDENT CODEX REVIEW`
