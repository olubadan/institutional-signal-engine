# Sanitized Prompt Record — SESSION-0009

- **Timestamp:** `20260804T161926Z`
- **Scope:** Begin Phase 3 signal-only vertical slice on the merged Phase 2 main commit.
- **Sanitization:** The owner-provided SSH connection was represented as
  `[REDACTED_CONNECTION]`. No credentials, tokens, keys, headers, or secret values
  are retained here.
- **Required outcome:** Recover repository/GitHub/VM context, create
  `feat/signal-only-vertical-slice`, implement fixture-testable signal-only
  components, commit/push intentional progress, and pause once for protected
  credential entry before provider authentication verification.

## Continuation prompt — 20260804

```text
Credential entry is complete on the existing Vast VM.

Continue SESSION-0009 on branch feat/signal-only-vertical-slice and draft PR #3.

1. Read AGENTS.md, CURRENT_STATE.md, JOURNAL_INDEX.md and the active SESSION-0009 record before acting.
2. Connect through the existing SSH configuration.
3. Verify `/etc/institutional-signal-engine/runtime.env` is owned by root:root with mode 0600.
4. Verify these variables are populated by presence only:
   - ALPACA_API_KEY_ID
   - ALPACA_API_SECRET_KEY
   - THETADATA_API_KEY
   - OPTIONS_API_BASE_URL
5. Confirm `TRADING_ENABLED=false`.
6. Never print, echo, copy, log, journal or commit the runtime file or any secret value.
7. Fix the Alpaca configuration so `ALPACA_DATA_FEED=iex` is treated as a feed name and never interpreted as a WebSocket URL. Add a regression test.
8. Verify `OPTIONS_API_BASE_URL` uses the local ThetaData v3 event endpoint.
9. Determine whether Theta Terminal is installed and running on the Vast VM. If it is missing, install and configure it from current official ThetaData documentation without exposing credentials.
10. Authenticate Alpaca and ThetaData with sanitized success/failure reporting.
11. Run provider-contract, reconnection, signal, persistence and deterministic-replay tests.
12. Run a short signal-only live smoke test and report feed health, received-event counts, latency, stale events, rejection reasons and ranked-signal counts.
13. Do not construct or submit paper or live orders.
14. Keep internal services loopback-only.
15. Update the sanitized SESSION-0009 record, current state, traceability and PR #3.
16. Commit and push clean changes to the existing Phase 3 branch.
17. Do not merge PR #3.

If blocked, stop with one precise manual action. Otherwise report the latest commit, tests, provider health and smoke-test results without revealing credentials.
```

## Restoration authorization continuation — 20260804

```text
I explicitly authorize secure restoration of the six missing internal PostgreSQL and Redis variables from the currently running containers into /etc/institutional-signal-engine/runtime.env.

Continue SESSION-0009.

Restore only:

- POSTGRES_DB
- POSTGRES_USER
- POSTGRES_PASSWORD
- REDIS_PASSWORD
- DATABASE_URL
- REDIS_URL

Requirements:

- Never print, echo, log, journal or commit their values.
- Preserve all existing Alpaca and ThetaData fields without displaying them.
- Preserve TRADING_ENABLED=false.
- Keep the file root:root mode 0600.
- Create a protected backup before mutation and securely remove it after successful verification.
- Verify only variable presence and service connectivity.
- Restart and verify PostgreSQL, Redis and their systemd dependency service.
- Confirm persistence survives restart.
- Authenticate Alpaca and ThetaData with sanitized reporting.
- Run the bounded signal-only live smoke test.
- Run all lint, typing, provider, signal, persistence and replay tests.
- Update SESSION-0009, current state, traceability and draft PR #3.
- Commit and push clean results.
- Do not expose ports, construct orders, enable trading or merge PR #3.

Report the latest commit, service health, provider authentication, smoke-test counts and any remaining blocker without revealing credentials.
```
