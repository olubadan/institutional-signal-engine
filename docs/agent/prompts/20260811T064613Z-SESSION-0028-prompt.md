# SESSION-0028 Prompt

Perform one final bounded evidence-bundle correction on
`feat/rth-orchestration-recovery`, stacked as draft PR #6 on unchanged draft PR
#5, starting exactly at `56bab0f4b2e35b22b3492576cee8ad4584943da7`.

Replace the circular in-journal persistence and replay assertions with the
authoritative lifecycle:

`J = seal(compose(E_det, Sigma))`

`P = persist(J)`

`R = replay(P)`

`Omega = (J, P, R)`

`Certificate = pi_CDelta(Omega)`

The immutable versioned acceptance contract `CDelta` supplies only expected
values and rules. The sealed journal supplies observed runtime facts, the
immutable persistence receipt supplies observed persistence facts, and the
immutable replay receipt supplies reconstructed replay facts. Build evidence
remains outside the runtime certificate.

Close causal and semantic verification by requiring stable operation identities,
exact parent/child correlation and payload relationships, a complete allowed
material vocabulary, lifecycle uniqueness and ordering, epoch/restoration/
command/acknowledgement/boundary/event consistency, finalization as the terminal
journal record, and cross-object run/contract/object binding. Reconstruct replay
only from the persisted serialized artifact and compare seal, chain, digests,
bytes, counts, roots, and projections before issuing `R`.

Implement the pure acceptance projection over immutable `CDelta` and verified
`Omega` only. Preserve the deterministic E1 `{A}`, E2 `{A,B}`, E3 `{B}` session,
paired TRADE/QUOTE subscriptions, acknowledgement-gated activation, active and
removed event handling, independent clock reevaluation, E2 restoration,
stop/drain/finalization, deterministic repeated artifacts, disabled trading, and
orders `0/0`.

Replace or extend adversarial coverage so every negative mutates `J`, `P`, `R`,
`CDelta`, or persisted serialization and traverses the production verifier and
projection with an exact failure code. Include wrong same-kind parent, coherently
rehashed unknown and fabricated accepted records, duplicates, run mismatches,
bad restoration and boundary membership, forged replay facts, unsealed replay,
wrong J/P bindings, altered receipt digests, projection inequality, and records
after finalization while retaining earlier genuine negatives.

Do not change strategy, physics, thresholds, `CONTROL_V1`, `SHADOW_IMPACT_V1`,
provider execution, PR #5, trading, orders, PR readiness, merge state, or Phase
5. Run the complete requested local suite, execute certification twice and
compare all artifacts, publish one coherent correction commit to PR #6, wait for
exact-head CI, and update only affected evidence with the final head and J/P/R/
certificate hashes.
