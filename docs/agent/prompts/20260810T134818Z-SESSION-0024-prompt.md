# SESSION-0024 Prompt

Repair the Phase 4B deployment ignition harness so its PostgreSQL, Alpaca,
persistence, and planner checks use the same typed production paths as the
live runner. Verify hermetically and in exact-head CI, deploy the verified
head to the existing VM, and launch only a passing partial-session recovery
observation during remaining regular-session hours. Keep trading disabled,
orders at 0/0, PR #5 draft/unmerged, and do not retry ThetaData REST/OI.

