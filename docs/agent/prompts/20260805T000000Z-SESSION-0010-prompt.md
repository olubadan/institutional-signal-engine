# SESSION-0010 Prompt

Continue Phase 3 on `feat/signal-only-vertical-slice` and correct the eight
merge-blocking implementation defects identified in the owner review. Preserve
signal-only operation, `TRADING_ENABLED=false`, sanitized append-only records,
and the draft, unmerged PR #3. Treat the formal mathematical model as
authoritative; do not invent absent formulas. Implement and test the specified
stateful indicators, persistence/replay pipeline, continuous evaluation,
candidate counters, correlated ThetaData acknowledgements, processing-time
latency/staleness, dynamic contract selection ports, and GitHub Actions CI.
Run all deterministic quality checks, update the state/traceability/journal
records, commit intentional changes, push the branch, and report blockers
honestly.
