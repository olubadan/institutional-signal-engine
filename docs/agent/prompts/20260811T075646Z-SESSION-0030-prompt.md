# SESSION-0030 Prompt

Correct the fused certification boundary on draft PR #6, starting exactly at
independently rejected head
`55146c7dc64d65ab452ee6d932287b2db18c3a3a`, while leaving draft PR #5
unchanged at `4a3afe8dd70fecc1b629ff7ef5614a652a572529`.

Remove the evidence capability boundary entirely. There must be exactly one
production certificate-construction path:

`Certificate = certify_repository_backed(CDelta, J, P, R, repository)`

Every invocation must validate sealed J, treat P and R only as untrusted
claims, load P's exact content-addressed object from the repository, compare
canonical bytes, reconstruct a distinct journal, independently verify its seal,
chain, structure, closed semantics, run, contract, count, root and digest,
independently derive the observed projection and replay facts, compare R to
those derived facts, and immediately construct the certificate from verified
local results.

Remove `VerifiedEvidencePackage`, `_issue`,
`_REPOSITORY_VERIFICATION_AUTHORITY`, authority checks, externally callable
projection over caller-created evidence, repository-free certificate
constructors, and any expected/receipt/cached/self-consistent value path into
accepted observations. Keep schema validation, receipt-integrity validation,
repository-backed operational verification, and authoritative issuance clearly
separate; schema validity must not imply operational proof.

Before implementation, enumerate every function that can produce a certificate,
`overall: PASS`, or an accepted certification value. Add production-boundary or
construction-path adversarial tests for former capability/lookalike
construction, `_issue`, low-level object forging, copied/mutated evidence,
forged authority, empty/wrong/altered repository objects, invented replay facts,
cross-run and cross-contract evidence, residual projection helpers, schema-only
manual certificates, and repository-load bypass. Require stable failure codes.

Preserve the deterministic `{A} -> {A,B} -> {B}` trace, causal-journal and
closed-semantic verification, paired acknowledgements, clock reevaluation,
recovery/finalization behavior, J/P/R separation, mathematics and thresholds,
`CONTROL_V1`, `SHADOW_IMPACT_V1`, provider isolation, disabled trading, orders
`0/0`, PR #5, PR #6's draft/unmerged state, and the Phase 5 boundary. Do not run
live providers.

Run Ruff format check and lint, strict mypy, dependency-lock verification, the
complete hermetic and focused certification suites, structural, shell, and
secret checks, deterministic repeated certification, and public API/construction
path inspection. Publish one coherent correction commit to PR #6, wait for
exact-head push and PR CI, and update the PR description with the exact head,
current counts, hashes, and CI links. Do not claim independent acceptance.
