# SESSION-0012 Prompt

Fix only the two final independent sweep-review findings on draft PR #3:
remove fixture-only exchange aliases so production participation uses only
authoritative numeric ThetaData codes, and add exact 30-minute freshness-boundary
coverage including pipeline persistence and deterministic replay. Run the
specified local checks, push the focused correction, and verify GitHub Actions
on the exact head. Keep the PR draft, trading disabled, and do not merge or
begin Phase 4.
