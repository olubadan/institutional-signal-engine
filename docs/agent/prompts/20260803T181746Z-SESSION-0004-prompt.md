# Prompt Record — SESSION-0004

- **Session ID:** `SESSION-0004`
- **UTC start time:** `20260803T181746Z`
- **Starting branch:** `chore/engineering-baseline-bootstrap`
- **Starting commit SHA:** `5d83a387d0598bdc19fe4b1bda8448a2c55fa801`
- **Intended objective:** Verify and merge approved PR #1, then perform Phase 2
  read-only Vast offer selection research and publish a ranked candidate report in
  a new draft PR.
- **Authorized scope:** Normal merge commit for PR #1; new Phase 2 branch; redacted
  Vast authentication check; read-only offer inspection; candidate analysis;
  documentation, journal, commit, push, and one draft PR.
- **Explicit prohibitions:** No Vast rental, provisioning, modification, or
  destruction; no charge; no provider credentials; no application, infrastructure,
  or trading implementation; no live trading.
- **Required stopping point:** Stop after publishing and reporting ranked Vast
  candidates, leaving the Phase 2 PR draft and awaiting explicit machine selection.

## Exact user prompt

```text
Begin SESSION-0004 for `olubadan/institutional-signal-engine`.

I have reviewed corrected draft PR #1 and explicitly approve its merge.

1. Read `AGENTS.md`, `docs/agent/CURRENT_STATE.md`, `docs/agent/JOURNAL_INDEX.md`, and the completed SESSION-0003 record.
2. Verify live PR #1 is still:

   * open and draft;
   * cleanly mergeable;
   * based on `main`;
   * headed by commit `5d83a387d0598bdc19fe4b1bda8448a2c55fa801`;
   * documentation-only;
   * free of credentials, Python implementation, tests, packaging, CI, infrastructure provisioning, and trading code.
3. If the live state matches, merge PR #1 into `main` using a normal merge commit so its development and journal history remains intact.
4. Verify the resulting authoritative `main` commit and confirm PR #1 is merged.

Then begin Phase 2.

5. Create `chore/vast-environment-bootstrap` from the verified merged `main`.
6. Check Vast.ai authentication without displaying credentials or sensitive account information.
7. Inspect currently available Vast offers read-only.
8. Identify the best 3–5 candidates meeting or exceeding:

   * Ubuntu 24.04 compatibility
   * 4 vCPU minimum; 8 preferred
   * 16 GB RAM minimum; 32 GB preferred
   * at least 100 GB persistent storage
   * reliable networking
   * stable host/reliability indicators
   * Docker compatibility
   * economical CPU-oriented operation
   * GPU not required
9. For every candidate report:

   * offer or machine ID
   * CPU model and vCPU count
   * RAM
   * available storage
   * network upload/download performance
   * reliability score
   * location
   * hourly price
   * estimated monthly price using 730 hours
   * strengths
   * selection rationale
10. Rank the candidates and recommend one preferred option.
11. Write the timestamped results to `docs/environment/VAST_CANDIDATES.md`.
12. Record SESSION-0004 concisely: commands, material results, merge evidence, candidate evidence, decisions, blockers, and next action. Do not reproduce repetitive terminal output.
13. Commit and push the Phase 2 branch and open one draft PR containing the candidate report and journal update.

STOPPING POINT

Do not rent, provision, modify, or destroy any Vast instance.

Do not request or configure Alpaca or options-provider credentials.

Do not implement application or trading code.

Stop after presenting the ranked Vast candidates and wait for my explicit machine selection and authorization to create a charge.

Report:

* merged PR number
* authoritative `main` commit
* Phase 2 branch
* candidate-report commit
* draft PR URL
* ranked Vast candidates
* recommended candidate
* exact hourly and estimated monthly cost
* exact next owner decision required
```
