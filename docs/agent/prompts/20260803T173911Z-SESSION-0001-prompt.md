# Prompt Record — SESSION-0001

- **Session ID:** `SESSION-0001`
- **UTC start time:** Unavailable; the original session did not capture a UTC
  start timestamp. `20260803T173911Z` is the Phase 1 recovery-record timestamp,
  not an invented Phase 0 start time.
- **Starting branch:** No local checkout; remote default branch `main`
- **Starting commit SHA:** `cbec064121faeb82245004e69f316179380a8e62`
- **Intended objective:** Perform Phase 0 only: inspect the repository and current
  environment, determine whether an engineering-baseline branch or draft PR
  exists, report current state, and propose the smallest correct next action.
- **Authorized scope:** Read-only repository and environment inspection.
- **Explicit prohibitions:** No paid Vast provisioning, credentials, production
  trading logic, merge, live trading, or Phase 1 implementation.
- **Required stopping point:** Stop after the Phase 0 report and recommended next
  action.

## Exact user prompt

```text
You are the lead implementation engineer for:

olubadan/institutional-signal-engine

MISSION

Build an end-to-end, production-oriented Real-Time Institutional Signal Engine using GitHub as the source of truth and a persistent Vast.ai instance as the development and runtime environment.

Operate autonomously within the defined scope. Convert this mission into an implementation plan, execute it phase by phase, verify every material result, and report progress through GitHub draft pull requests.

PRIMARY OUTCOME

Create a working system that:

1. Receives real-time equity market data from Alpaca.
2. Receives real-time options data from the options-data provider that I will supply.
3. Normalizes and synchronizes those data streams.
4. Computes the formal signal model defined in the repository.
5. Detects valid, fresh, executable institutional signals.
6. Ranks qualifying candidates deterministically.
7. Supports paper-trading execution through Alpaca.
8. Records signals, decisions, orders, fills, system health, and performance.
9. Runs reproducibly on a persistent Vast.ai environment.
10. Is developed, tested, documented, reviewed, and released through GitHub.

OPERATING AUTHORITY

You are authorized to:

* Inspect the GitHub repository and identify its current state.
* Create feature branches.
* Select a suitable Vast.ai instance from the options accessible to me.
* Configure the selected Vast environment.
* Install the required development and runtime dependencies.
* Clone and configure the repository on Vast.
* Add application code, infrastructure configuration, tests, documentation, and CI workflows.
* Commit and push approved work to feature branches.
* Open draft pull requests.
* Run tests and paper-trading simulations.
* Recommend the next implementation phase.

Do not merge pull requests or deploy live trading without my explicit approval.

GITHUB RULES

* GitHub is the authoritative source of truth.
* Never commit directly to the default branch.
* Use focused feature branches.
* Keep commits intentional and reviewable.
* Open a draft pull request for each major phase.
* Include acceptance evidence in every pull request.
* Do not merge any pull request.
* Do not rewrite or delete unrelated user work.
* Do not commit credentials, API keys, tokens, private certificates, or `.env` files.
* Add safe `.env.example` files containing variable names only.
* Use GitHub Actions secrets only when CI requires credentials.
* Stop and ask me before making an irreversible or unexpectedly destructive change.

VAST ENVIRONMENT TARGET

Select and configure a persistent Vast.ai development instance that meets or exceeds:

* Ubuntu 24.04
* 8 vCPU preferred; 4 vCPU minimum
* 32 GB RAM preferred; 16 GB minimum
* At least 100 GB persistent storage
* Reliable network connectivity
* Docker Engine
* Docker Compose
* Python 3.12
* `uv`
* Git
* GitHub CLI
* PostgreSQL client
* Redis client
* Build tools and required system libraries

A GPU is not required for the initial signal engine unless a confirmed component needs one. Prefer a stable, economical CPU-oriented instance with sufficient memory and persistent disk.

Before renting or provisioning an instance that creates a new charge, show me the best candidates, their specifications, and their prices, then wait for my selection or approval.

SECREDENTIAL HANDLING

I will provide:

* Alpaca API credentials
* The real-time options API credentials
* Provider URLs or subscription details when required

When credentials are needed:

1. Tell me the exact environment-variable names required.
2. Tell me where I should enter them securely.
3. Do not ask me to paste credentials into source code, documentation, GitHub issues, commits, pull requests, or chat output intended for permanent storage.
4. Store runtime credentials only in an appropriate secret mechanism or a protected `.env` file on Vast with restrictive permissions.
5. Ensure secret files are excluded by `.gitignore`.
6. Redact sensitive values from logs and command output.
7. Confirm configuration by reporting only whether each credential is present and whether authentication succeeded.
8. Never display or repeat a complete secret.

INITIAL EXECUTION PLAN

Phase 0 — Ground the Current State

* Verify access to the GitHub repository.
* Identify the default branch.
* Inspect existing branches, files, pull requests, workflows, and documentation.
* Read `AGENTS.md` and all relevant repository instructions.
* Determine what has already been completed.
* Do not duplicate or overwrite existing work.
* Produce a concise gap analysis against this mission.

Phase 1 — Engineering Baseline

If the baseline bootstrap has not already been completed:

* Create or use the branch `chore/engineering-baseline-bootstrap`.
* Add the approved documentation and engineering structure.
* Add development conventions, architecture boundaries, acceptance criteria, and a requirements traceability structure.
* Commit the work.
* Push the branch.
* Open one draft pull request.
* Stop for review before proceeding if this phase changes the repository’s foundational structure.

Phase 2 — Vast Development Environment

* Inspect the Vast.ai capabilities available to me.
* Present suitable instance candidates if a rental decision is required.
* After approval, provision the selected persistent instance.
* Install and verify all required dependencies.
* Clone the repository.
* Authenticate GitHub securely.
* Create `docs/environment/VAST_SETUP.md`.
* Add reproducible setup scripts or infrastructure configuration where appropriate.
* Verify that a clean environment can reproduce the setup.
* Commit through a feature branch and open a draft pull request.

Phase 3 — Application Architecture

Implement a modular architecture with clear boundaries for:

* Configuration and secrets
* Alpaca market-data adapter
* Options-data provider adapter
* Symbol universe management
* Event normalization
* Time synchronization
* Signal-state computation
* Validity gate `S`
* Freshness gate `F`
* Room/capacity gate `R`
* Executability gate `E`
* Deterministic candidate ranking
* Paper-order execution
* Position and exit management
* Persistence
* Observability
* Replay and backtesting
* Testing

Prefer replaceable provider interfaces so that data vendors can be changed without rewriting the signal engine.

Phase 4 — Data Integration

* Connect to Alpaca’s paper environment first.
* Connect to the supplied real-time options API.
* Validate authentication without exposing credentials.
* Build resilient streaming clients.
* Add reconnection, backoff, heartbeat, stale-data detection, rate-limit handling, and structured error reporting.
* Normalize timestamps, symbols, prices, volumes, option volume, open interest, call premium, spreads, market state, and sector state.
* Record provenance and event timestamps for every signal input.
* Add a replayable event format for deterministic testing.

Phase 5 — Formal Signal Engine

Implement the repository’s mathematical specification faithfully:

* Liquidity indicator
* Options-flow indicator
* Equity-direction indicator
* Market-direction indicator
* Sector-direction indicator
* Signal validity `S(i,t)`
* Freshness `F(i,t)`
* Room and capacity `R(i,t)`
* Executability `E(i,t)`
* Candidate set `C(t)`
* Lexicographic candidate selection
* Fire decision
* Deterministic no-candidate state

Make thresholds configurable and versioned. Preserve deterministic behavior for identical input streams.

Create unit tests for every equation, boundary condition, gate transition, tie-break rule, and system invariant.

Phase 6 — Paper-Trading Execution

* Use Alpaca paper trading only.
* Implement entry-price protection.
* Implement configurable take-profit behavior.
* Implement fixed-horizon and freshness-loss exits.
* Implement position sizing and concurrent-position capacity.
* Record intended orders, submitted orders, acknowledgements, fills, rejections, exits, execution costs, and realized returns.
* Add kill-switch and trading-disabled modes.
* Default to trading disabled until explicitly enabled for paper trading.
* Do not enable live trading.

Phase 7 — Persistence and Observability

Implement:

* PostgreSQL-backed event and decision history
* Redis where useful for transient state or coordination
* Structured JSON logging
* Health and readiness checks
* Data-feed health
* Signal counts
* Gate rejection reasons
* Stream latency
* Stale-data metrics
* Order and fill metrics
* Position exposure
* Execution errors
* Daily performance summaries

Every execution decision must be traceable back to the exact normalized inputs, configuration version, and signal-engine version that produced it.

Phase 8 — Verification

Create:

* Unit tests
* Integration tests
* Contract tests for provider adapters
* Deterministic replay tests
* Failure and reconnection tests
* Paper-trading acceptance tests
* Formal invariant tests
* CI checks
* Linting, formatting, and type checking
* Reproducible local and Vast execution instructions

Do not claim success based only on code compilation. Verify behavior with test evidence and authenticated paper-environment connectivity.

PHASE COMPLETION FORMAT

At the end of each phase, report:

* Phase completed
* Branch name
* Commit SHA
* Draft pull-request number and URL
* Files created or changed
* Tests run
* Test results
* Acceptance criteria satisfied
* Remaining blockers
* Decisions requiring my approval
* Exact next recommended action

FIRST ACTION

Begin with Phase 0 only.

Inspect the repository and the current environment. Determine whether the engineering-baseline branch or draft pull request already exists. Report the current state and propose the smallest correct next action.

Do not provision a paid Vast instance, enter credentials, write production trading logic, merge anything, or begin live trading during Phase 0.
```
