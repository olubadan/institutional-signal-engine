# SESSION-0020 Prompt

Diagnose and correct only the Phase 4 smoke startup timeout on `feat/phase-4-live-universe` at head `1cd271998b00a0487a9ef37e03f461bcbb30b057`. Reproduce startup with hermetic instrumentation, add flushed sanitized stage markers and configurable startup/network timeouts, bound the total command, add regression tests, run local quality checks, push intentional changes, and verify exact-head CI. Do not run another live smoke, retry ThetaData REST/OI endpoints, alter strategy or selection logic, construct orders, or change documentation while validation is running.
