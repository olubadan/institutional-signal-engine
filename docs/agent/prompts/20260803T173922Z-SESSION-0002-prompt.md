# Prompt Record — SESSION-0002

- **Session ID:** `SESSION-0002`
- **UTC start time:** `20260803T173922Z` (first captured repository-local session timestamp)
- **Starting branch:** Remote `main`; local feature branch created after live-state verification
- **Starting commit SHA:** `cbec064121faeb82245004e69f316179380a8e62`
- **Intended objective:** Establish the append-only journal, recover Phase 0 as
  `SESSION-0001`, and execute the approved Phase 1 engineering baseline through a
  draft PR.
- **Authorized scope:** Journal, engineering baseline, requirements traceability,
  baseline checks, focused commits, feature-branch push, and one draft PR.
- **Explicit prohibitions:** No merge, paid Vast provisioning, credentials,
  production trading logic, destructive unrelated changes, or live trading.
- **Required stopping point:** Stop after the Phase 1 draft PR and completed journal
  are visible on GitHub.

## Exact user prompt

```text
Approve Phase 1, with the following permanent project-journal requirement.

Before implementing Phase 1, establish an append-only, timestamped Codex operating journal inside the repository.

Create:

docs/agent/
README.md
CURRENT_STATE.md
JOURNAL_INDEX.md
NEXT_PROMPT.md
prompts/
sessions/

Use UTC timestamps formatted as:

YYYYMMDDTHHMMSSZ

Every Codex session must receive a sequential session ID:

SESSION-0001
SESSION-0002
SESSION-0003

For each session, create:

docs/agent/prompts/<timestamp>-<session-id>-prompt.md

docs/agent/sessions/<timestamp>-<session-id>.md

PROMPT RECORD

The prompt file must preserve:

* Session ID
* UTC start time
* User’s exact prompt
* Starting branch
* Starting commit SHA
* Intended objective
* Authorized scope
* Explicit prohibitions
* Required stopping point

SESSION RECORD

The session file must preserve, in chronological order:

* Session ID
* Start and completion timestamps
* Exact prompt received
* Initial repository state
* Codex commentary and progress reports
* Commands executed
* Sanitized command output
* Files inspected
* Files created or modified
* Decisions made
* Reasons for material decisions
* Tests and verification
* Errors encountered
* Recovery actions
* User approvals received
* Scope changes
* Branch name
* Commit SHA
* Pull-request number and URL
* Final Codex response
* Unresolved blockers
* Exact next recommended action
* Proposed next Codex prompt

Preserve enough command output to establish what happened and why. Do not replace meaningful evidence with vague summaries.

Where complete output is excessively long, preserve:

1. The command exactly as executed.
2. The relevant output.
3. The exit status.
4. The location of any full generated log or test artifact.
5. A digest of omitted repetitive output.

Never record secrets, credentials, API keys, authorization headers, private keys, `.env` values, or sensitive tokens. Replace them with `[REDACTED]`.

CURRENT STATE

Maintain `docs/agent/CURRENT_STATE.md` as the authoritative snapshot containing:

* Project mission
* Current phase
* Completed phases
* Active branch
* Latest commit
* Open pull request
* Current architecture
* Vast environment state
* Provider-integration state
* Test state
* Approved decisions
* Pending approvals
* Known blockers
* Next action

JOURNAL INDEX

Maintain `docs/agent/JOURNAL_INDEX.md` as a chronological table containing:

* Sequence number
* Session ID
* Timestamp
* Objective
* Branch
* Commit
* Pull request
* Outcome
* Link to prompt
* Link to session record

Never rewrite or delete prior session records. Corrections must be added as a new timestamped entry.

NEXT PROMPT

At the end of each session, place the complete proposed next instruction in:

docs/agent/NEXT_PROMPT.md

The prompt must be self-contained but must also direct the next Codex session to read:

1. `AGENTS.md`
2. `docs/agent/CURRENT_STATE.md`
3. `docs/agent/JOURNAL_INDEX.md`
4. The most recent session record
5. All applicable repository instructions

SESSION START RULE

At the beginning of every future session:

* Determine the latest session ID.
* Read the authoritative state and latest journal entry.
* Compare the documented state with live GitHub state.
* Report any discrepancy before changing the repository.
* Create the next sequential session record.
* Never rely only on conversational memory.

SESSION COMPLETION RULE

Before ending every session:

* Run the relevant tests.
* Run `git status`.
* Confirm the branch.
* Confirm the commit SHA.
* Confirm the draft pull-request state.
* Complete the session record.
* Update `CURRENT_STATE.md`.
* Update `JOURNAL_INDEX.md`.
* Update `NEXT_PROMPT.md`.
* Commit and push the journal updates to the active feature branch.
* Confirm that the journal files are visible on GitHub.

PHASE 0 RECOVERY

Use the current CLI session and verified GitHub findings to create `SESSION-0001` as the Phase 0 record.

Preserve the original Phase 0 prompt, commands, relevant outputs, findings, gap analysis, repository commit, and recommended Phase 1 action.

Do not invent anything that was not observed. Mark unavailable details explicitly.

PHASE 1 EXECUTION

After establishing the journal:

* Create `chore/engineering-baseline-bootstrap` from the verified `main` commit.
* Add only the approved engineering-baseline and requirements-traceability structure.
* Include the agent-journal structure described above.
* Run baseline checks.
* Commit intentional changes.
* Push the feature branch.
* Open one draft pull request.
* Do not merge.
* Stop after the draft pull request and journal are visible on GitHub.

Report:

* Session ID
* Branch
* Commit SHA
* Draft PR number and URL
* Journal files created
* Baseline files created
* Tests performed
* Results
* Blockers
* Exact next action
```
