# Prompt Record — SESSION-0008

- **Session ID:** `SESSION-0008`
- **UTC start time:** `20260803T190342Z`
- **Starting branch:** `chore/vast-environment-bootstrap`
- **Starting commit SHA:** `6289991f22c110773d96cf16b533f7218dba5ce9`
- **Intended objective:** Take ownership of the already-running Vast instance,
  implement and verify the reproducible Phase 2 development/runtime environment,
  clone the Phase 2 branch, and update draft PR #2.
- **Authorized scope:** Connect using existing SSH authentication; inspect before
  mutation; install required tools; configure restart-safe development services;
  clone and verify the repository; add infrastructure, documentation, traceability,
  and sanitized journal evidence.
- **Explicit prohibitions:** No credential disclosure or request unless genuinely
  blocked, no live trading, no PR merge, and no additional Vast instance.
- **Required stopping point:** Stop when the existing VM is working, persistence
  and documented checks pass, and the Phase 2 branch/PR is updated, or provide one
  precise manual action for a genuine credential/external authorization blocker.

## User prompt

The prompt below is exact except that the public SSH connection string is
security-redacted in this committed journal.

```text
The Vast.ai VM is now running and SSH access has been verified.

Connection:
ssh -p [REDACTED_SSH_PORT] root@[REDACTED_PUBLIC_HOST]

Instance:
- Vast instance ID: 46725769
- Ubuntu 22.04
- 20 vCPU
- 64 GB RAM
- 130 GB disk
- $0.108/hour
- Maximum duration: 28 days

Take ownership of Phase 2 and advance the actual project.

Connect to the VM using the existing SSH configuration. Inspect what has already been installed before making changes. Then:

1. Securely bootstrap the Ubuntu environment.
2. Install the project’s required system tools and runtime dependencies.
3. Clone `olubadan/institutional-signal-engine` into `/opt/institutional-signal-engine`.
4. Check out the existing Phase 2 branch associated with PR #2.
5. Run the repository’s documented setup, lint, type-check, test, and build commands.
6. Implement the remaining Phase 2 Vast infrastructure work necessary for a reproducible development/runtime environment.
7. Configure services for persistent operation and restart recovery where the repository specification requires them.
8. Verify connectivity and runtime health.
9. Update the infrastructure documentation and requirements traceability with the actual instance configuration and verification evidence.
10. Commit and push the work to the existing Phase 2 branch and update PR #2.

Security requirements:
- Do not print, journal, or commit credentials.
- Do not request secrets in chat or command arguments.
- Create a root-owned environment-file template and tell me separately which secret variable names must be populated.
- Do not start live trading.
- Do not merge the pull request.
- Do not provision another Vast instance.

Maintain a sanitized, timestamped execution journal in the repository so another Codex session can recover full project context. Continue autonomously through implementation and verification. Stop only if a credential or external authorization is genuinely required, and then provide one precise manual action.
```
