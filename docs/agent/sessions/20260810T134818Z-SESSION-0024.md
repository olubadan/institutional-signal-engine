# Session Record — SESSION-0024

## Identity and scope

- **Session ID:** `SESSION-0024`
- **Start timestamp:** `20260810T134818Z`
- **Branch:** `feat/phase-4b-impact-shadow`
- **Starting head:** `ce34759a3878ab4c2905f3851b709db8afe67414`
- **Pull request:** [#5](https://github.com/olubadan/institutional-signal-engine/pull/5), open and draft
- **Objective:** Correct deployment ignition to exercise production configuration,
  typed Alpaca authentication, real persistence drain, and the live planner;
  then recover the remaining regular-session observation if ignition passes.

## Work log

Implementation and verification evidence will be appended chronologically.

## Diagnosis

- The original ignition used host `pg_isready` while PostgreSQL is
  containerized and used a Redis CLI probe without the protected runtime
  connection path.
- It used a separate raw Alpaca HTTP request instead of the typed
  `AlpacaEquitiesProvider.current_prices()` path.
- The persistence result was derived from the database boolean and did not
  enqueue or drain a writer record.
- The plan checks read an optional manifest and never ran the production
  discovery, enrichment, and `build_coverage_plan()` path.

## Correction

- Ignition now uses `Settings`, `PostgresRepository`, the typed Alpaca
  provider, the typed MDDS status adapter, and an asynchronous reversible
  persistence probe.
- The live runner exposes a prepare-only planner pass and writes a protected
  ordered plan artifact. A live run can consume that artifact and fails closed
  if its planner output differs.
- Hermetic regression coverage covers typed authentication, paired-plan
  validation, persistence drain, empty/over-capacity plans, and the zero-order
  safety invariant.

## Verification

The focused ignition/assembled-runner tests pass. Full-suite and exact-head
CI evidence will be appended after the correction is pushed. No live provider
session has been launched by this session yet.
