# Session Record — SESSION-0024

## Identity and scope

- **Session ID:** `SESSION-0024`
- **Start timestamp:** `20260810T134818Z`
- **Branch:** `feat/phase-4b-impact-shadow`
- **Starting head:** `ce34759a3878ab4c2905f3851b709db8afe67414`
- **Pull request:** [#5](https://github.com/olubadan/institutional-signal-engine/pull/5), open and draft
- **Objective:** Correct deployment ignition to exercise production configuration,
  typed Alpaca authentication, real persistence drain, and the live planner;
  then recover the remaining regular-session observation if ignition passes.

## Work log

Implementation and verification evidence will be appended chronologically.

## Diagnosis

- The original ignition used host `pg_isready` while PostgreSQL is
  containerized and used a Redis CLI probe without the protected runtime
  connection path.
- It used a separate raw Alpaca HTTP request instead of the typed
  `AlpacaEquitiesProvider.current_prices()` path.
- The persistence result was derived from the database boolean and did not
  enqueue or drain a writer record.
- The plan checks read an optional manifest and never ran the production
  discovery, enrichment, and `build_coverage_plan()` path.

## Correction

- Ignition now uses `Settings`, `PostgresRepository`, the typed Alpaca
  provider, the typed MDDS status adapter, and an asynchronous reversible
  persistence probe.
- The live runner exposes a prepare-only planner pass and writes a protected
  ordered plan artifact. A live run can consume that artifact and fails closed
  if its planner output differs.
- Hermetic regression coverage covers typed authentication, paired-plan
  validation, persistence drain, empty/over-capacity plans, and the zero-order
  safety invariant.

## Verification

The focused ignition/assembled-runner tests pass. Full-suite and exact-head
CI evidence will be appended after the correction is pushed. No live provider
session has been launched by this session yet.

## Post-session harvest

- The ignition/prepared-plan run ID was `0950e1c9-0e67-42b2-9f55-98d545650e52`.
  It persisted one preflight universe manifest row and zero canonical events,
  decisions, consumed quotes, sweeps, transitions, or finalization records.
- The attempted recovery command started at 09:57:30 ET / 13:57:30 UTC and
  stopped after the second discovery/enrichment pass before WebSocket
  connection. The preserved log ended with a sanitized `RuntimeError` after
  enrichment; the prepared-plan validation in the runner identifies this
  failure point as `prepared_plan_mismatch`. The preflight enrichment stage
  reported 911 completed quote results; the recovery pass reported 912,
  demonstrating that the repeated provider pass did not reproduce the exact
  prepared plan.
- No subscriptions, accepted events, clusters, synchronized inputs,
  decisions, replay, or live CONTROL/SHADOW comparison were produced. The
  original log remains unchanged at mode 0600. Trading stayed disabled and
  orders remained `0/0`.
- The 20-symbol preflight funnel was 10,387 discovered, 10,387 mapped,
  925 coarse-selected/enrichment-requested, and 40 coverage-selected:
  `U_M=0`, `U_X=0`, `U_R=40`, `U*=40`. Final roots were BAC, NFLX, NVDA,
  TSLA, and WMT. Fifteen symbols reached zero final contracts because the
  persisted selection reason was `final_liquidity_evidence_failed`; no live
  terminal-state evidence exists for them because the observation never
  reached subscription.
- This is `PARTIAL_SESSION_INTEGRATION_EVIDENCE` only in the sense of a
  failed recovery attempt, not a market observation, and it does not count
  toward the five-session/30-footprint requirement. The smallest correction
  is to make the ignition-generated plan the authoritative consumed plan (or
  persist all inputs needed to avoid a second provider discovery/enrichment
  pass), then rerun exact-head CI and a fresh pre-open session.
