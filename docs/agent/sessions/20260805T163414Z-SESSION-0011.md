# Session Record — SESSION-0011

## Identity and scope

- **Session ID:** `SESSION-0011`
- **Start timestamp:** `20260805T163414Z`
- **Branch:** `feat/signal-only-vertical-slice`
- **Pull request:** draft PR #3, open and unmerged
- **Objective:** Correct throughput, quote conflation, material evaluation,
  asynchronous persistence, and deterministic replay under the owner-specified
  governing principle.

## Starting evidence

- Starting head: `4693ed1ede88aa735edd3b0e4417076b12ffd7f6`.
- Trading remains disabled; execution remains absent.
- ThetaData dynamic discovery remains an external HTTP 500 blocker and will not
  be retried during this session.

## Work log

Implementation and verification evidence will be appended chronologically.

## Throughput implementation and verification — 20260805

- Added `quote_book.py`: latest-value slots keyed by provider/instrument,
  bounded event-time windows, newest-valid-at-or-before-trade selection,
  explicit `CURRENT_STATE`, `TRADE_CLASSIFICATION`, and `BOTH` roles, and
  received/current-state-overwrite/consumed/pending counters. Overwrites count
  slot-replacement operations and are intentionally not a mutually exclusive
  lifecycle bucket; consumed and pending are unique quote identities, with
  `consumed + pending <= received` as the lifecycle invariant.
- Added `persistence_async.py`: bounded asyncio audit queue, batched writer,
  soft-limit warnings, hard-limit fail-closed state, queue-depth and database
  latency metrics, and complete shutdown draining.
- Material-change reasons are persisted with each decision, including price,
  RVOL, relative-strength, premium, room, spread, ask-side, quote-validity,
  eligible-trade, and freshness triggers. Unchanged synchronized states are
  counted as skips and do not create decisions.
- PostgreSQL now persists reconstructible consumed quotes and migrates decision
  trigger/state fields. `replay_with_consumed_quotes()` replays exact consumed
  quote inputs deterministically.
- Local checks passed: Ruff format/lint, strict mypy, lock verification, diff
  checks, and 34 hermetic tests. Vast checks passed the same quality suite.

## Final live throughput run — 20260805

- Run `b9e0f47a-a16d-422f-b389-0386d66634ef`: Alpaca/Theta authentication and
  Theta acknowledgement succeeded. Event counts by provider/kind were Alpaca
  quotes 10,368, Alpaca trades 107, Theta quotes 8, and Theta trades 8.
  Quotes received/current-state overwrites/consumed were `10,376/10,374/91`;
  quotes pending at shutdown were reported separately. Overwrites are slot
  replacement operations and may overlap consumed/pending identities; they are
  not lifecycle totals. The lifecycle invariant is consumed plus pending less
  than or equal to received. Trades
  received/processed were `115/115`.
- Evaluations triggered/skipped were `5/101`; skip reason was unchanged
  synchronized state. Stale, late, duplicate, and out-of-order counts were
  `0/0/0/0`; unknown conditions were `5`; candidate counters were `1/0/0/0`.
- Internal queue-wait and processing targets passed. Queue depth p50/p95/max
  was `1/4/11`; database-write latency p50/p95/max was
  `20.355/25.078/36.394 ms`; soft-limit crossings and hard-limit failures were
  zero. Per-provider/event-kind age, queue-wait, processing, and total-age
  distributions are preserved in the sanitized smoke result.
- PostgreSQL contained 115 persisted trade events, 91 consumed quote records,
  and 5 decisions. Replay consumed 115 trades and 91 quotes, produced 5
  decisions, and matched persisted decisions field by field (`True`).
- Trading remained disabled; orders constructed/submitted were `0/0`. PR #3
  remains draft and unmerged. ThetaData discovery HTTP 500 remains awaiting
  support and was not retried.

## Publication closure — 20260805

- Implementation head: `07587abfe348a95b7625dca92f999e25b4068361`.
- Documentation closure commit: `12d435cc78a5fb17870ef67672af129823177b5a`.
- Journal record correction commit: `0421fca66150ca27ba962d8be5594353d3a5de4b`.
- GitHub Actions passed on the implementation/documentation head:
  `https://github.com/olubadan/institutional-signal-engine/actions/runs/31026802182`.
  The PR-context rerun also passed:
  `https://github.com/olubadan/institutional-signal-engine/actions/runs/31026806064`.
- PR #3 is open, draft, clean, and unmerged. No credentials, orders,
  infrastructure, service exposure, or Phase 4 work occurred.

## Counter-semantics correction — 20260805

- Renamed `quotes_superseded` to `current_state_overwrites`. It counts slot
  replacement operations, not unique quote identities, and therefore may
  overlap a quote later consumed for classification or current state.
- `quotes_received` counts every parsed quote message; `quotes_consumed`
  counts unique quote identities recorded in the consumed-input audit; and
  `quotes_pending_at_shutdown` counts unique latest-slot quote identities not
  consumed at shutdown. The lifecycle invariant is
  `quotes_consumed + quotes_pending_at_shutdown <= quotes_received`.
- The regression test proves both the invariant and intentional overwrite
  overlap. No report presents overwrite operations as a mutually exclusive
  lifecycle total.

## Indicator and boundary closure pass — 20260805

- Commit `36a8626` adds typed historical bootstrap through Alpaca’s official
  stock-bars endpoint with `adjustment=split`, completed-session exclusion,
  daily/minute provenance, and cached session baselines. It wires Decimal
  previous-close delta, VWAP, same-minute RVOL, SPY/sector relative strength,
  hierarchical resistance, and namespaced provenance into synchronized state
  and persisted decisions.
- Session-boundary handling is ET-aware, idempotent, resets session state at
  regular open, records open/close reasons, and is invoked by the live smoke
  timer. Missing baseline values fail closed; uncalculated Alpaca payloads do
  not reach decisions. Resistance levels/provenance freeze per session while
  distance and selected overhead level recompute for current price.
- Validator findings were corrected: resistance source wiring, relative-
  strength gate use and same-minute alignment, excluded/out-of-session
  handling, placeholder rejection, resistance cache semantics, source-date
  provenance, completed-session RVOL profiles, initial-boundary reason order,
  and timer-driven boundary coverage.
- Live run `4faad966-f4fb-44c4-8f2f-58ec440c59c6`: bootstrap success;
  Alpaca 11,440 events; ThetaData 16 option events; exact-contract
  acknowledgement success; trades `59/59`; synchronized evaluations 3;
  stale/late/duplicate/out-of-order `0/0/0/0`; trigger reasons included
  `session_boundary_open`; unknown conditions 6; counters `1/0/0/0`; orders
  `0/0`.
- PostgreSQL run-scoped counts were 59 events, 61 consumed-quote records, and
  3 decisions. Deterministic replay produced 3 decisions and matched every
  persisted decision field. The 43-test hermetic suite, Ruff, and strict mypy
  passed.
- This earlier blocker statement was superseded by the owner-authorized sweep
  closure below. ThetaData discovery HTTP 500 remains external and was not
  retried.

## Final engineering review — 20260805

- Implementation head: `b991f95091fee78397adfadf4125dda4671f2094`.
- Corrected quote audit reuse: one quote can now produce multiple
  trade-classification consumption records, one per distinct trade, while
  `quotes_consumed` remains a unique-quote metric. Added
  `test_one_quote_can_be_audited_for_multiple_trade_classifications`.
- Verification passed: Ruff format/lint, strict mypy, 35 hermetic tests, and
  GitHub Actions run
  `https://github.com/olubadan/institutional-signal-engine/actions/runs/31028561845`.
  The test fixture prohibits internet sockets for every unmarked test, and no
  live integration tests are present.
- Confirmed from sanitized prior-run evidence: accepted trades processed and
  retained `115/115`; PostgreSQL had 115 events and 5 decisions; replay
  matched decisions field by field; quote lifecycle invariant was
  `consumed + pending <= received`; orders constructed/submitted `0/0`.
- The earlier findings on indicator wiring and absent sweep triggers were
  superseded by the owner-authorized indicator and sweep closure commits.
  Dynamic ThetaData discovery remains an external HTTP 500 blocker, while
  fixture selection/reconciliation and supplied exact-contract streaming
  remain separately evidenced.

## Owner-authorized sweep implementation — 20260805

- Added fixed 1,000 ms event-time, exact-contract call sweep clustering with
  three-valid-exchange participation, Decimal premium, ask/bid/unknown
  directional evidence, 65% ask and 50% unknown limits, and the separate
  three-sweep/$500,000 session gate.
- Added one-shot qualification, revocation/requalification, 30-minute
  freshness expiry, session reset, deterministic correction/cancellation
  recomputation, uncorrelated-correction fail-closed diagnostics, and closed
  cluster audit persistence with stable IDs and provenance.
- Added regression tests for all core boundaries and transitions, including
  exact timestamp/premium boundaries, exchange uniqueness, zero classified
  premium, session threshold separation, freshness, corrections, and final
  audit state, including freshness expiry after a cluster closes. Local
  checks passed: 55 hermetic tests, Ruff format/lint, and strict mypy.
- No live qualifying sweep is claimed unless naturally observed in the
  bounded feed. Discovery HTTP 500 remains awaiting support and was not
  retried. Trading remains disabled; no orders were constructed or submitted;
  PR #3 remains draft and unmerged.

## Bounded live sweep verification — 20260805

- Run `9b88bcab-d37b-4e2e-b047-35fd1e402c02` ran during regular hours using
  successful historical bootstrap and the supplied AAPL 2026-08-07 $310 call
  exact-contract Standard stream. Alpaca authentication, ThetaData
  authentication, and the correlated acknowledgement succeeded; `STREAM_BULK`
  was not used. No qualifying sweep naturally occurred, so live qualification
  is not claimed.
- Alpaca/ThetaData event counts were `9,222/44`; trades received/processed
  `98/98`; stale/late/duplicate/out-of-order `0/0/0/0`; evaluations
  triggered/skipped `9/82`; candidate counters `1/0/0/0`; orders `0/0`.
  Unknown conditions were `15`. Quote received/overwrite/consumed/pending
  were `9,168/9,166/90/1`, satisfying the documented lifecycle invariant.
- Run-scoped PostgreSQL contained 98 events, 102 consumed-quote records, 9
  decisions, and 5 sweep audits. Replay produced 9 decisions and matched
  persisted decisions field by field. Discovery HTTP 500 remains awaiting
  support and was not retried.
