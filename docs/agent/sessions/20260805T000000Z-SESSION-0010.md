# Session Record — SESSION-0010

## Identity and scope

- **Session ID:** `SESSION-0010`
- **Start timestamp:** `20260805T000000Z`
- **Branch:** `feat/signal-only-vertical-slice`
- **Pull request:** draft PR #3, open and unmerged
- **Objective:** Correct the eight implementation defects identified in the
  Phase 3 engineering review while preserving signal-only safety.

## Starting evidence

- GitHub and the local checkout agree on the feature branch and PR #3.
- Main remains anchored at `a3b34c5166040797123e943c23f0428344198ac3`.
- The formal model was reviewed before implementation. It does not define the
  eligible-trade set, ask-side classification, or five-day resistance/high
  formula; these must not be silently invented.

## Work log

Implementation and verification evidence will be appended chronologically.

## Implementation evidence — 20260805T000000Z

- Added `indicators.py` with Decimal previous-close return, session VWAP,
  time-of-day RVOL baseline, relative-strength arithmetic, session reset,
  provenance-bearing historical ports, and explicit missing-data reasons.
- Added deterministic option trade classification from quote context and
  stateful session cumulative volume/premium enrichment in `pipeline.py`.
- Connected continuous accepted-event processing to repository event and
  decision writes; added typed PostgreSQL event replay reads and decision/event
  count methods.
- Added four candidate counters to the decision schema and signal evaluation.
- Corrected ThetaData acknowledgement handling to correlate request ID,
  contract, add state, and connection generation; unmatched responses are
  diagnosed and cannot acknowledge subscriptions. Standard mode contains no
  `STREAM_BULK` route. The smoke contract is explicit, not an operational
  default.
- Added dynamic option-universe ports, liquid-call fixture selection, plan-limit
  enforcement, and subscription reconciliation primitives.
- Added GitHub Actions CI for frozen uv dependencies, lock verification, Ruff,
  strict mypy, hermetic pytest, shell validation, structural checks, and secret
  scanning. Unit tests prohibit network sockets while allowing isolated Unix
  event-loop sockets.
- Local checks: Ruff format/check passed, strict mypy passed, `uv lock --check`
  passed, `git diff --check` passed, and 22 pytest tests passed.

## Specification boundary

The authoritative mathematical model still lacks definitions for the eligible
trade set, ask-side classification, and five-day resistance/high distance.
The implementation therefore retains raw trade/quote context, classifies only
when quote evidence exists, and fails closed with explicit reason codes. No
claim of full indicator verification or dynamic live discovery is made while
ThetaData discovery helpers return HTTP 500.

## VM verification — 20260805T1003–1120 ET

- The VM checkout was synchronized through a non-secret Git bundle to the
  pushed feature commit. It was clean. Runtime inspection proved root ownership,
  mode `0600`, required-field presence, and `TRADING_ENABLED=false`; no values
  were displayed. Docker, the dependency service, PostgreSQL, Redis, and
  loopback MDDS were healthy.
- VM deterministic checks passed: frozen uv sync, Ruff format/check, strict
  mypy, Bash syntax, ShellCheck when available, and 22 pytest tests.
- The bounded 60-second command completed. Sanitized result: Alpaca
  authentication success, 18,182 Alpaca events; ThetaData authentication
  success, 78 option trades, terminal stream connected, but no correlated
  subscription acknowledgement observed; 14,269 synchronized evaluations;
  stale 334, late 0, duplicate 359, out-of-order 86; provider transport
  latency median/max `15806.604/30004.723 ms`; processing latency median/max
  `0.010/0.111 ms`; candidate counters `1/0/0/0`; explicit missing-feature and
  S/F/R/E rejection reasons; zero orders constructed or submitted.
- Sanitized PostgreSQL queries showed cumulative counts of 63,188 canonical
  events and 30,544 decisions; typed event replay returned 63,188 events and
  typed decision reads returned 30,544 decisions.

## Remaining verification blockers

- The live ThetaData connection status was not treated as an acknowledgement
  because the response did not correlate to the outstanding request ID and
  contract. This is correct fail-closed behavior; live ack verification needs
  a correlated Terminal response.
- Stored PostgreSQL data includes earlier runs recorded under the old
  timestamp-based decision identity. A field-by-field equality comparison of
  the entire historical decision set against new deterministic replay is not
  claimed; a run-scoped audit/replay key is required before this acceptance
  item can be verified.
- Hosted CI run `31014294645`
  (https://github.com/olubadan/institutional-signal-engine/actions/runs/31014294645)
  passed for the latest pushed documentation head `ecdaa74`.

## Publication evidence — 20260805T1125Z

- Published implementation commits through `9acf413` and sanitized state/
  session evidence in `ec28f91`.
- GitHub Actions run
  `https://github.com/olubadan/institutional-signal-engine/actions/runs/31014294645`
  passed for the latest pushed head `ecdaa74`.
- PR #3 remains open, draft, and unmerged. No trading, order route, service
  exposure, infrastructure provisioning, or Phase 4 work occurred.

## Owner-definition closure — 20260805T1047–1200 ET

- Implemented the owner-authoritative ThetaData numeric condition mapping from
  the official Trade Conditions table:
  `https://docs.thetadata.us/Articles/Errors-Exchanges-Conditions/Trade-Conditions.html`.
  Mapping version is `thetadata-trade-conditions-v3-20260805`. The exact
  numeric map 0–97 is checked into `thetadata_conditions.py`; excluded sets are
  multi-leg `{35,36,37,38,67}`, cancellations `{40,41,42,43,44}`, late/out-of-
  sequence `{2,5,6,13,15,26,27,28,56,57,65}`, and corrections `{61,77}`.
  Unknown codes increment a sanitized counter and fail closed.
- Implemented exact Decimal ask-side classification and quote validity:
  positive/locked quotes, quote-at-or-before-trade, age at most 500 ms,
  identity match, price within one cent of ask, and strictly above midpoint.
  Boundary regressions cover locked/crossed/zero/midpoint/spread/stale/future/
  mismatched quotes.
- Implemented cached split-adjusted resistance ladder selection for prior
  five, twenty, and 252 completed sessions, including frozen provenance and
  explicit `NO_OVERHEAD_RESISTANCE` state without infinity or magic distance.
- Added run IDs, deterministic ingest order, deterministic decision order, and
  condition mapping version to persisted decisions.
- Corrected the actual Terminal acknowledgement shape: `REQ_RESPONSE` uses
  `header.req_id`; the successful live response had header keys
  `req_id,response,status,type` and no top-level ID. Correlation remains
  request-ID and contract strict.
- Final corrected live smoke run ID:
  `eba46d34-4335-486d-9929-33863f44b331`. Results: 15,763 Alpaca events, 60
  Theta option trades, acknowledgement success, 15,131 synchronized
  evaluations, stale 0, late 0, duplicate 312, out-of-order 130, unknown
  condition 13, provider transport latency median/max `7125.503/19314.564 ms`,
  processing latency median/max `0.010/0.110 ms`, counters `1/0/0/0`, and zero
  orders constructed/submitted.
- Run-scoped PostgreSQL query returned 15,381 accepted events and 15,131
  decisions. Replay returned 15,131 decisions; field-by-field equality was
  `True`, with mapping version `thetadata-trade-conditions-v3-20260805`.

## Final verification handoff — 20260805T1205Z

- Local quality checks passed: Ruff format/check, strict mypy, 27 hermetic
  pytest tests, frozen lock verification, diff check, and secret scan. VM
  formatting/lint, strict typing, pytest, Bash syntax, and ShellCheck also
  passed before the final smoke.
- The only remaining provider blocker is ThetaData contract/expiration/strike
  discovery returning HTTP 500. The supplied contract remains smoke-only; no
  claim of dynamic live discovery is made.
- PR #3 remains draft and unmerged. No order was constructed or submitted,
  trading stayed disabled, and Phase 4 did not begin.
## Closure-pass correction — 20260805

This append-only correction supersedes earlier provisional wording in this
record about replay equality. Run-scoped deterministic replay succeeded for run
eba46d34-4335-486d-9929-33863f44b331. PostgreSQL contained 15,381 accepted
events and 15,131 decisions. Replay produced 15,131 decisions with
field-by-field equality. The proof applies to this corrected run under its
recorded engine, configuration and condition-mapping versions; it does not
claim that every historical run is replayable.

The smoke telemetry now preserves source, adapter receive and pipeline
processing timestamps for each event and reports independent distributions for
Alpaca trades, Alpaca quotes, ThetaData option trades and ThetaData option
quotes. Event age at receipt and processing duration are separate measurements;
raw values are not clamped or combined across providers. Timestamp conversion
flags identify NY/UTC and ThetaData millisecond-of-day conversion.

Discovery evidence is separated as follows. Fixture verified: expiration,
strike and contract parsing, liquidity selection, 15,000-contract enforcement,
subscription reconciliation and reconnect restoration. Live verified: the
supplied exact-contract Standard subscription, correlated acknowledgement and
live option trades. Not live verified: contract, expiration and strike
discovery and fully dynamic live universe selection. The discovery HTTP 500
remains awaiting ThetaData support; the failing endpoints were not repeatedly
called during this closure pass.

Unknown-condition rejection is reported only as live evidence of fail-closed
handling. Unit boundary tests separately cover each S/F/R/E component. Live
candidate counters and run-scoped replay determinism are separate evidence.

## Final closure verification — 20260805T1615Z

- Closure commit `ddfa96adfe60e7251805cad5880bb0c635e57097` was pushed. Local
  and Vast Ruff format/lint, strict mypy, lock verification, diff checks, Bash
  syntax, ShellCheck, and 29 hermetic tests passed. GitHub Actions passed at
  `https://github.com/olubadan/institutional-signal-engine/actions/runs/31023522485`.
- Final run `14a4ab65-3c3b-449a-aa49-d6eaf30dd352` authenticated Alpaca and
  ThetaData, correlated the exact-contract acknowledgement, and recorded
  15,845 Alpaca events and 38 option events. It produced 15,058 synchronized
  evaluations, stale 0, late 0, duplicate 345, out-of-order 153, unknown
  conditions 8, candidate counters 1/0/0/0, and orders 0/0.
- Run-scoped PostgreSQL contained 15,385 accepted events and 15,058 decisions.
  Replay produced 15,058 decisions and field-by-field equality was `True`.
- VM clock synchronization was yes and its system timezone was UTC. The
  Terminal reported CONNECTED; no reconnect, queued-message, or buffering
  indication appeared in the inspected log window. Raw timing distributions
  remain split by provider and event kind in the smoke evidence; no values were
  clamped or combined. The Vast region was not exposed by the runtime probe,
  and plan-level throttling was not established by official documentation or
  support confirmation.
- PR #3 remains draft and unmerged. Trading remained disabled; no order was
  constructed or submitted and Phase 4 did not begin.
