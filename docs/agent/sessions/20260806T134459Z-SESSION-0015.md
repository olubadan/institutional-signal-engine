# Session Record — SESSION-0015

## Identity and scope

- **Session ID:** `SESSION-0015`
- **Start timestamp:** `20260806T134459Z`
- **Branch:** `feat/phase-4-live-universe`
- **Pull request:** draft PR #4
- **Objective:** Apply the owner-approved Phase 4 observation policy
  amendments without enabling trading or starting Phase 5.

## Starting evidence

- Starting head: `2edf386734951c2e912fd695585e622e0db29d32`.
- PR #4 is draft and unmerged; exact-head CI is green.
- Trading is disabled. ThetaData REST discovery remains unavailable and
  cross-validation-only.

## Work log

Evidence will be appended chronologically.

## Implementation and verification — 20260806

- Added versioned policy `phase4-observation-liquidity-relaxation-v1`. The
  symbol-level source is `OWNER_APPROVED_PHASE4_PILOT` and
  `symbol_liquidity_verified=false`; selected contracts inherit this metadata.
  The policy is enforced by the Phase 4 selector and rejected when another
  policy version is supplied.
- Positive undated Alpaca OI is accepted only for Phase 4 observation and is
  propagated as `oi_date_source=ALPACA_UNDATED`,
  `open_interest_verified_as_of=false`, and
  `evidence_quality=PHASE4_OBSERVATIONAL` into synchronized inputs and
  persisted decision provenance. Missing or zero OI remains fail-closed.
- Added deterministic separate TRADE/QUOTE capacity allocation with nearest
  expiration, moneyness ordering, symbol round-robin, per-symbol limits,
  provider budgets, and immutable `SUBSCRIPTION_CAPACITY_EXCLUDED` records.
  Reports and manifests distinguish requested, selected, capacity-excluded,
  submitted, acknowledged, and rejected counts.
- Local verification passed: Ruff format/lint, strict mypy, and 104 hermetic
  tests. No credentials were inspected or recorded; trading remains disabled.
