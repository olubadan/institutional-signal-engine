# Session Record — SESSION-0028

## Identity and scope

- **Session ID:** `SESSION-0028`
- **Start timestamp:** `20260811T064613Z`
- **Branch:** `feat/rth-orchestration-recovery`
- **Starting head:** `56bab0f4b2e35b22b3492576cee8ad4584943da7`
- **Pull request:** [#6](https://github.com/olubadan/institutional-signal-engine/pull/6), open and draft, stacked on PR #5
- **Objective:** Replace circular journal replay evidence with the immutable
  `Omega = (J, P, R)` evidence lifecycle and close exact causal, semantic, and
  run-binding validation.

## Reconciliation before implementation

- The worktree was clean and local HEAD, the remote branch, and PR #6 head all
  matched the required starting SHA.
- GitHub reported PR #6 open, draft, unmerged, and based on
  `feat/phase-4b-impact-shadow`; PR #5 was unchanged, open, draft, and unmerged at
  `4a3afe8dd70fecc1b629ff7ef5614a652a572529`.
- `CURRENT_STATE.md`, `JOURNAL_INDEX.md`, the latest SESSION-0027 record, and
  live GitHub agreed on the active branch, phase, PR topology, and published
  state. No discrepancy required correction before implementation.
- GitHub CLI authentication succeeded; credentials were not displayed or
  recorded.

## Scope constraints

Preserve strategy, physics, thresholds, `CONTROL_V1`, `SHADOW_IMPACT_V1`,
disabled trading, zero orders, provider isolation, PR #5, the draft/unmerged PR
#6 state, and the Phase 5 boundary. Runtime evidence is exactly `Omega = (J, P,
R)` under immutable `CDelta`; build and deployment evidence remain separate.

## Work log

### Implementation

- Replaced the circular journal tail with the authoritative lifecycle
  `J = seal(compose(E_det, Sigma))`, `P = persist(J)`, `R = replay(P)`,
  `Omega = (J, P, R)`, and `Certificate = pi_CDelta(Omega)`.
- `session.finalized` is now J's final record. The shell seals immediately after
  finalization, prohibits later appends, and makes no persistence-success or
  replay-success claim inside J.
- Added immutable, independently digested persistence and replay receipts. P is
  issued only after repository save succeeds and uses a content-addressed
  persistence object identity. Replay reads that object, reconstructs a new
  journal, reruns seal/digest/chain/semantic validation, and compares exact
  bytes, record counts, roots, and observed projections before R is created.
- Added immutable `PHASE4B_ACCEPTANCE_CONTRACT_V3` as the sole source of expected
  run identity, versions, record vocabulary, epoch membership, boundaries,
  deterministic events, restoration membership, lifecycle order, and required
  invariants. J/P/R remain the sole sources of observations.
- Added unique operation IDs, exact cause-operation IDs, and stable correlation
  IDs. Closed semantic verification now rejects unknown, duplicate,
  contradictory, misplaced, incorrectly correlated, and unexpected material
  records even when their full hash chain is coherently rebuilt.
- Replaced the one-journal projection with `pi_CDelta(Omega)`, which receives
  only immutable CDelta and a verified evidence package. Expected and observed
  values are separate in the certificate; Git, worktree, CI, PR, shell, and
  environment state remain absent.
- Expanded focused coverage to 44 tests. Every negative mutates J, P, R, CDelta,
  or persisted serialization, invokes the production verifier/projection, and
  asserts a stable failure code. Earlier genuine negatives remain covered.

### Verification

- `uv run ruff format --check .` — passed, 147 files already formatted.
- `uv run ruff check .` — passed.
- `uv run mypy src` — passed under the project-authoritative strict scope, 38
  source files.
- `uv lock --check` — passed, 33 packages resolved.
- `uv run pytest -q` — passed, 258 hermetic tests.
- `uv run pytest -q tests/test_phase4b_certify.py` — passed, 44 focused positive
  and genuine adversarial tests.
- `find infra -type f -name '*.sh' -print0 | xargs -0 -r -n1 bash -n` — passed.
  Local ShellCheck is unavailable; exact-head CI retains the authoritative
  ShellCheck gate.
- CI-equivalent structural checks, secret regex scan, `git diff --check`, and
  JSON parsing of all four artifacts — passed.
- Two independent complete executions produced byte-identical J, P, R, and
  certificate files; a third default-path execution matched both.
- J: `/tmp/phase4b-certification/JOURNAL.json`, SHA-256
  `02fa0820152556db2b6a3620224a77865238a393e83536a39399d886406522c1`;
  54 records; terminal kind `session.finalized`; root
  `721d43edcac9e4dff10b37cc6aa5a36653c4233fcd9cc162805d9840d20d2e34`.
- P: `/tmp/phase4b-certification/PERSISTENCE_RECEIPT.json`, SHA-256
  `c97d148f7bbae485be231df2d45714ee749ac80a7de58c3a56a2e96142369835`.
- R: `/tmp/phase4b-certification/REPLAY_RECEIPT.json`, SHA-256
  `5c8faad0f1c396d3c4ad00965365a4ca156233e89b112e06f3a469b352f524b1`;
  exact equality is true.
- Certificate: `/tmp/phase4b-certification/CERTIFICATE.json`, SHA-256
  `01dbcdcaca2e367aeac5db63bd9b3b4a1818e357d428215b4af9ec1111e40784`;
  overall `PASS`.

### Publication

- One coherent journal-closing correction commit contains the complete bounded
  change. Its exact SHA, exact-head GitHub CI, PR #6 evidence update, and final
  live PR states are confirmed externally after push because a commit cannot
  contain its own identity or subsequent CI result.
- No provider ran. Trading remained disabled; orders constructed/submitted
  remained `0/0`. PR #5 was not modified. PR #6 remains draft and unmerged.
