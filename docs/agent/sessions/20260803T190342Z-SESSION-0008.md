# Session Record — SESSION-0008

## Identity and scope

- **Session ID:** `SESSION-0008`
- **Start timestamp:** `20260803T190342Z`
- **Completion timestamp:** In progress
- **Starting branch:** `chore/vast-environment-bootstrap`
- **Starting commit:** `6289991f22c110773d96cf16b533f7218dba5ce9`
- **Active pull request:** [#2](https://github.com/olubadan/institutional-signal-engine/pull/2)
- **Exact prompt:** [Sanitized prompt record](../prompts/20260803T190342Z-SESSION-0008-prompt.md)

## Prompt received

The full prompt is preserved in the linked prompt record with only the public SSH
connection string redacted. It reports existing Vast instance `46725769` running
Ubuntu 22.04 with 20 vCPU, 64 GB RAM, 130 GB disk, `$0.108/hour`, and a 28-day
maximum, and authorizes Phase 2 bootstrap and verification without live trading,
PR merge, another instance, or secret disclosure.

## Initial repository state and discrepancy

- Read all required instructions and completed SESSION-0007.
- Live GitHub matched the journal: local/remote Phase 2 branch was clean at the
  starting commit; PR #2 was open, draft, cleanly mergeable, and based on `main`.
- External state differs from the prior journal: the owner reports instance
  `46725769` is running and SSH-verified, whereas SESSION-0007 correctly recorded
  that Codex had not created an instance. This owner-created instance is now the
  sole authorized Phase 2 target.
- Ubuntu 22.04 and the 28-day maximum differ from the original preferred Ubuntu
  24.04 persistent target. The owner's explicit instruction to advance this actual
  VM is treated as approval of those documented constraints, not permission to
  create another machine.

## Chronological evidence

```console
$ git fetch origin main chore/vast-environment-bootstrap
$ git status --short --branch
## chore/vast-environment-bootstrap...origin/chore/vast-environment-bootstrap
$ gh pr view 2 --repo olubadan/institutional-signal-engine --json ...
PR #2: OPEN, draft=true, CLEAN, base=main
head: 6289991f22c110773d96cf16b533f7218dba5ce9
exit: 0
```

## Decisions, changes, verification, and errors

In progress. SSH connection details and all authentication material will remain
redacted from committed records.

```console
$ ssh [REDACTED_CONNECTION] '<read-only OS/resource/tool/repository inventory>'
timestamp: 20260803T190431Z
SSH: succeeded as root
OS: Ubuntu 22.04 x86_64; systemd running
resources: 19 visible vCPU; 51,676,916 KiB RAM; 135,084,687,360-byte root filesystem
existing: Docker 29.7.1, Compose 5.4.0, Git 2.34.1, Python 3.10.12, Make, GCC, curl, jq
missing: uv, GitHub CLI, PostgreSQL client, Redis client
repository at /opt/institutional-signal-engine: absent
exit: 0
```

- Vast API independently reported instance `46725769` running, machine `25189`,
  Ubuntu 22.04, 20 effective vCPU, 130 GB disk, base `$0.0866666667/hour`, storage
  `$0.0216666667/hour`, and total `$0.1083333333/hour`. Public connection fields
  were excluded.
- A read-only dpkg formatting expression was interpreted by the remote shell under
  `set -u`, stopping the remainder of that inspection. No mutation occurred. The
  query was corrected; Docker/Compose, ext4, `/opt`, and SSH checks passed.
- The measured VM passes minimum CPU/RAM/disk requirements but not the documented
  Ubuntu 24.04 target. ADR-0001 records the owner-approved Ubuntu 22.04 and finite-
  duration exception.
- Added an idempotent bootstrap, Compose dependency stack, systemd restart unit,
  protected variable-name-only template, verification/persistence suite, Make
  quality gates, setup guide, actual-instance record, and traceability update.
- Installed the Ubuntu GitHub CLI package as the minimal private-clone
  prerequisite. The first stdin-token authentication command used an unsupported
  old-CLI flag and made no change. A corrected login stored authentication in a
  root-owned mode-`0600` CLI file, but that CLI's generated Git credential helper
  could not feed Git. Two failed clone attempts left no repository; a third left
  an empty directory, removed safely with `rmdir`.
- Recovery used a temporary one-shot `GIT_ASKPASS` script containing no token. The
  existing token traveled over SSH stdin and existed only in the remote process
  environment. The private repository then cloned successfully to `/opt` on the
  Phase 2 branch at `4fce1bb559103aec0019ab59ca5cc7730b9c9eec` with a clean remote
  URL.
- `make setup` completed: system clients, ShellCheck, pinned `uv` 0.11.28, Python
  3.12.13, protected environment files, SSH key-only hardening, and enabled Docker,
  unattended-upgrade, and dependency services were installed. No secret value was
  emitted.
- The first `make lint` failed on ShellCheck SC2034 for an unused retry-loop
  variable. The version-controlled script was corrected to evaluate its loop
  counter; the rerun passed.
- `make typecheck` passed Compose configuration and the project systemd unit. It
  emitted one unrelated Ubuntu warning for the preinstalled `snapd.service` using
  a newer `RestartMode` directive than systemd 249 recognizes.
- `make build` pulled PostgreSQL 16 Alpine and Redis 7 Alpine successfully.
- The first `make test` passed all resource, tool, access-control, branch, service,
  container health, connectivity, and restart-persistence checks, then falsely
  failed the SSH assertion. Independent `sshd -T` evidence showed password and
  keyboard-interactive authentication disabled and key-only root access. Root
  cause: `pipefail` observed `sshd`'s SIGPIPE when `grep` completed. Recovery
  captures the full effective configuration before assertions; rerun pending.
- The corrected full `make lint && make typecheck && make build && make test`
  sequence passed. A fresh SSH connection after the dependency restart succeeded;
  the systemd unit remained active and both containers were healthy.
- Post-test Git status exposed one generated `.state/` directory in the checkout.
  The sentinel was relocated to `/var/lib/institutional-signal-engine`, and the
  verified PostgreSQL/Redis images were pinned by digest. Final rerun pending.

## Final state

- **Vast instance:** `46725769`, owner-created, pending independent SSH inspection
- **Branch:** `chore/vast-environment-bootstrap`
- **Commit:** Pending
- **Pull request:** #2, draft
- **Tests and verification:** Pending
- **Errors and recovery:** None
- **Unresolved blockers:** None known
- **Exact next action:** Inspect the VM read-only over existing SSH access
- **Proposed next Codex prompt:** Pending
- **Final Codex response:** Pending
