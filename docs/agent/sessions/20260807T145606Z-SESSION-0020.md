# Session Record — SESSION-0020

## Identity and scope

- **Session ID:** `SESSION-0020`
- **Start timestamp:** `20260807T145606Z`
- **Branch:** `feat/phase-4-live-universe`
- **Starting head:** `1cd271998b00a0487a9ef37e03f461bcbb30b057`
- **Pull request:** draft PR #4
- **Objective:** Bound and instrument Phase 4 smoke startup without changing strategy, selection, sweep, gate, provider-discovery, or live-observation behavior.

## Initial state

- The corrected VM checkout was synchronized from a verified Git bundle to the required head and the temporary bundle was removed.
- VM preflight passed for the corrected head: Dockerized PostgreSQL and Redis healthy, MDDS CONNECTED, Alpaca authentication successful, trading disabled, and no prior Phase 4 process.
- The 600-second smoke started at `10:41:13 ET` but remained active for approximately 10m41s with an empty protected log. It was stopped without producing a run ID or live evidence; no extended observation began.
- No live provider call, REST discovery/OI retry, credential inspection, order construction, or order submission is authorized in this session.

## Implementation log

- Added `startup.py` with a flushed `StageRecorder`, canonical stage list,
  sanitized timeout records, and `bounded_startup()` based on
  `asyncio.wait_for`.
- Added `PHASE4_STARTUP_TIMEOUT_SECONDS` configuration and
  `--startup-timeout-seconds`. The phase4 CLI has a total command timeout of
  observation seconds plus bounded startup/drain allowance.
- Wrapped Alpaca historical/pricing and contract pagination, quote enrichment,
  database initialization, and MDDS status calls. ThetaData websocket and full
  subscription acknowledgement stages are emitted by the provider; the live
  signal smoke fails closed on an acknowledgement timeout and drains accepted
  persistence work.
- Corrected startup sequencing so the signal smoke emits `observation_started`
  and starts equity collection only after all requested subscriptions are
  acknowledged. This prevents partial startup from creating strategy decisions
  and makes the requested observation duration begin at successful startup.
- No strategy, selection, sweep, gate, threshold, discovery, OI, or execution
  behavior was changed. No live smoke was run and no provider REST endpoint was
  retried.

## Verification

- `uv run ruff format --check .`: passed.
- `uv run ruff check .`: passed.
- `uv run mypy --strict src`: passed.
- `uv run pytest -q`: 122 hermetic tests passed.
- `uv lock --check`: passed.
- Shell syntax, structural checks, and repository secret scan: passed.
- ShellCheck was unavailable on the local Mac; the GitHub Actions job remains
  required for shell validation.

## Evidence and remaining action

- The prior VM smoke produced no stage marker because the old implementation
  did not flush startup progress; its zero-byte output cannot identify the
  exact blocked provider call after the fact. This correction makes the first
  blocked stage and bounded timeout observable without exposing credentials.
- Starting another live smoke is intentionally deferred to the next regular
  session. PR #4 remains draft and unmerged; trading is disabled and orders are
  0/0.
