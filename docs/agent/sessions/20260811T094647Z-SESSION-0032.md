# Session Record — SESSION-0032

## Identity and scope

- **Session ID:** `SESSION-0032`
- **Start timestamp:** `20260811T094647Z`
- **Branch:** `feat/rth-orchestration-recovery`
- **Starting head:** `3caa6dfffe3b6b08ffdb89aeae2d0ed46e61a294`
- **Pull request:** [#6](https://github.com/olubadan/institutional-signal-engine/pull/6), open and draft, stacked on unchanged PR #5
- **Objective:** Complete one durable, replayable, offline-certifiable live
  observational-session path while preserving the distinct accepted hermetic
  Phase 4B certificate.

## Reconciliation before implementation

- `JOURNAL_INDEX.md` ended at `SESSION-0031`; this is the next sequential,
  append-only session.
- Local HEAD, `origin/feat/rth-orchestration-recovery`, and draft PR #6 all
  matched the independently accepted head
  `3caa6dfffe3b6b08ffdb89aeae2d0ed46e61a294`.
- Live GitHub confirmed PR #6 open, draft, unmerged, mergeable, and CI-green.
  PR #5 remained open and draft at
  `4a3afe8dd70fecc1b629ff7ef5614a652a572529` with passing CI.
- The local worktree was clean. Documented branch, phase, PR topology, and
  safety state matched live GitHub. `CURRENT_STATE.md` named independent
  validation as the prior next action; this owner instruction supersedes that
  next action without correcting a historical discrepancy.
- GitHub CLI authentication succeeded; credentials were not displayed or
  recorded.

## Frozen complete-path and failure inventory

1. **Invocation and preflight:** one unambiguous full-session mode; valid market
   date and ET boundaries; exact commit/application/schema/config/resource
   identities; disabled trading and zero orders; fresh non-aliased output;
   explicit reachable durable repository; no provider connection before every
   precondition passes. Reject smoke/full ambiguity, unsafe paths, identity
   gaps, and unavailable durability.
2. **Recoverable evidence lifecycle:** allocate one session ID and incomplete
   staging area; checkpoint and fsync causal evidence before or with effects;
   retain diagnosable incomplete evidence after crash, signal, disk-full, or
   partial write; never publish a completed manifest or certificate on failure.
3. **One provider composition:** discovery, enrichment, subscription control,
   control acknowledgements, and data events share one connection authority and
   running receiver. Separate control sockets and empty event contract
   authorities are forbidden.
4. **Subscription transition state:** journal unique commands before send;
   correlate responses through the receiver; require paired TRADE/QUOTE
   acknowledgements before activation; represent pending, active, removing, and
   inactive states. Missing, rejected, malformed, unmatched, duplicate,
   stale-generation, or disconnect-interrupted responses fail closed.
5. **Inbound event authority:** admit option events only for the exact active
   generation, contract, and channel after activation. Journal pre-activation,
   removed, malformed, duplicate, late, and pending-removal traffic as rejected.
   Empty, stopped, or disconnected inbound authority cannot complete.
6. **Clock and universe:** independently wait/tick from 09:30 through 16:00 ET,
   record host-clock observations and drift, rediscover and enrich on schedule,
   persist epochs/diffs, acknowledge additions before admission, and acknowledge
   removals before inactivity with deterministic pending-traffic handling.
7. **Recovery:** receiver failure records disconnect, invalidates transitions,
   creates a new connection generation, restores the active epoch with fresh
   paired acknowledgements, and resumes only afterward. Termination, exhausted
   retries, or incomplete restoration is incomplete failure.
8. **Shutdown:** stop intake exactly at 16:00 ET, close intake deterministically,
   settle control transitions, drain accepted events and writes to quiescence,
   finalize once, and terminally seal the causal journal.
9. **Durable bundle:** persist the sealed journal and final state; bind session,
   market, clock, provider, connection, discovery/enrichment, subscription,
   event, epoch, shutdown, safety, fallback, code/config/schema/resource, record
   count/root/seal, and persistence/replay facts; publish the manifest atomically
   only when complete. Never overwrite existing artifacts.
10. **Offline replay and certification:** accept one finalized bundle path;
    validate manifest, identities, trusted bindings, component hashes, causal
    chain, acknowledgements/recovery, shutdown, and safety; replay persisted J;
    compare independently derived final state; issue a fresh replay receipt;
    publish the separate observational certificate last. Reject missing,
    reordered, changed, coherently rehashed but untrusted, mixed, copied,
    cross-session/contract/version, premature, altered-receipt, trading, or order
    evidence.
11. **Executable operations:** exact immutable-head, environment, launch, health,
    fail-closed termination, inspection, replay, and certification commands;
    CLI-help/runbook tests prevent drift.
12. **Hermetic production-path proof:** the same composition and clock loop runs
    two accelerated 09:30–16:00 sessions containing discovery, paired
    acknowledgements, events, reevaluation/admission/removal,
    disconnect/reconnect/restoration, 16:00 stop, drain, seal, durability,
    replay, and live certification; deterministic fields receive byte and
    semantic equality checks.

## Scope constraints

Do not contact providers, access production credentials, change PR #5, merge,
begin Phase 5, enable trading, construct or submit orders, or reopen the accepted
hermetic certification model or authority boundary.

## Work log

### Implementation

- Added the separate live observational evidence subsystem and pinned validation
  rules resource. The production boundary requires exact Git identity, fresh
  staged output, explicit durable persistence, protected authority material,
  disabled trading, and zero orders before provider connection.
- Added one unified command/acknowledgement/event port and production Theta
  adapter. Commands are journaled with request identity before transmission;
  paired acknowledgements are consumed on the same receive path; activation,
  removal, disconnect/reconnect restoration, and event admission are gated by
  acknowledged generation state.
- Added independent ET clock control through 16:00, deterministic drain and
  terminal finalization, continuous checkpoints, durable journal reload,
  persistence/replay receipts, HMAC-bound manifest-last publication, and
  offline inspect/replay/certify commands. The live certificate is explicitly
  observational and separate from the accepted hermetic certificate.
- Added the virtual-clock harness and adversarial tests, and replaced the stale
  Monday run card with exact head, environment, launch, health, fail-closed,
  inspection, replay, and certification commands.

### Verification evidence

- Ruff format/lint, strict mypy, `uv lock --check`, Bash syntax, secret scan,
  and `git diff --check` passed.
- Complete hermetic suite passed: 287 tests. Focused live-session suite passed:
  9 tests. Two accelerated harness executions completed with byte-identical
  configuration, final-state, journal, manifest, persistence, replay, replay
  output, and certificate artifacts. The harness exercised discovery,
  acknowledgement-gated paired subscriptions, events, add/remove reevaluation,
  controlled disconnect/reconnect/restoration, 16:00 stop, drain, finalization,
  persistence, replay, and live certification.
- No provider ran; no production credentials were accessed; trading remained
  disabled; orders constructed/submitted remained `0/0`; PR #5 was not
  modified; PR #6 remains draft and unmerged; Phase 5 did not start.

### Remaining handoff

The implementation/tests/affected operational documentation were committed as
`1205afcafe612958d37789965f01f3bf704da7b9` and pushed only to PR #6's branch.
Exact-head CI jobs `31480520321` and `31480517224` passed. PR #6 is open, draft,
mergeable, and unmerged; PR #5 remains unchanged, open, and draft at
`4a3afe8dd70fecc1b629ff7ef5614a652a572529`. The journal-close commit records
this final state. Independent validation remains pending; the build result is
not a provider or host readiness claim.
