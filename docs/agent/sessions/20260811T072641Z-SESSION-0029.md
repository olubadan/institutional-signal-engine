# Session Record — SESSION-0029

## Identity and scope

- **Session ID:** `SESSION-0029`
- **Start timestamp:** `20260811T072641Z`
- **Branch:** `feat/rth-orchestration-recovery`
- **Starting head:** `e7026dcb803277ec56359ca4ed7d0a8ad9ddcb76`
- **Pull request:** [#6](https://github.com/olubadan/institutional-signal-engine/pull/6), open and draft, stacked on unchanged PR #5
- **Objective:** Make evidence-bundle verification repository-authoritative so
  P and R cannot establish persistence or replay facts without reconstructing J
  from the repository.

## Reconciliation before implementation

- `JOURNAL_INDEX.md` ended at `SESSION-0028`; this record is the next sequential
  append-only session.
- Live GitHub confirmed PR #6 open, draft, unmerged, and at the required rejected
  starting head `e7026dcb803277ec56359ca4ed7d0a8ad9ddcb76`, based on PR #5's
  unchanged head `4a3afe8dd70fecc1b629ff7ef5614a652a572529`.
- The local worktree was clean and content-identical at the correct head, but
  detached while `CURRENT_STATE.md` documented the active branch. This was
  reported before editing and corrected by reattaching the checkout to the
  existing `feat/rth-orchestration-recovery` branch.
- Phase, PR topology, remote heads, and draft/unmerged state otherwise matched
  the journal. GitHub CLI authentication succeeded; credentials were not
  displayed or recorded.

## Scope constraints

Preserve strategy, mathematics, thresholds, `CONTROL_V1`, `SHADOW_IMPACT_V1`,
the deterministic three-epoch causal trace, provider isolation, disabled
trading, zero orders, PR #5, draft/unmerged PR #6, and the Phase 5 boundary.

## Work log

### Implementation

- Replaced the public evidence-package dataclass with an opaque capability whose
  constructor fails closed and whose repository-verification authority is
  required by certificate projection.
- Added the production `verify_repository_backed_evidence_package` boundary. It
  receives the repository, validates P as a claim, loads P's object, deserializes
  and fully verifies a distinct journal, compares exact canonical bytes with J,
  independently derives replay facts, and compares caller-supplied R.
- The production command passes the same in-memory repository used to persist J
  into verification. J, P, R, the deterministic trace, and the certificate
  schema remain unchanged.
- Added production-path adversarial coverage for absent, wrong, byte-different,
  cross-run, and cross-contract objects; direct and unverified packages;
  caller-invented replay facts; and internally consistent forged receipts.

### Verification

- `uv run ruff format --check .` — passed, 149 files formatted.
- `uv run ruff check .` — passed.
- `uv run mypy src` — passed under strict project scope, 38 source files.
- `uv lock --check` — passed, 33 packages resolved.
- `uv run pytest -q` — passed, 267 hermetic tests.
- `uv run pytest -q tests/test_phase4b_certify.py` — passed, 53 focused tests.
- Bash syntax, CI-equivalent structure/secret scan, `git diff --check`, and JSON
  parsing of all artifacts passed. Local ShellCheck was unavailable; the
  exact-head CI workflow installs and runs it.
- Two fresh complete executions produced byte-identical J, P, R, and certificate
  files. Certificate overall is `PASS`; trading is disabled; orders are `0/0`.
- J SHA-256:
  `02fa0820152556db2b6a3620224a77865238a393e83536a39399d886406522c1`.
- P SHA-256:
  `c97d148f7bbae485be231df2d45714ee749ac80a7de58c3a56a2e96142369835`.
- R SHA-256:
  `5c8faad0f1c396d3c4ad00965365a4ca156233e89b112e06f3a469b352f524b1`.
- Certificate SHA-256:
  `01dbcdcaca2e367aeac5db63bd9b3b4a1818e357d428215b4af9ec1111e40784`.

### Publication

- One coherent correction commit contains implementation, adversarial tests,
  current evidence, and this append-only journal closure. Its exact SHA and
  exact-head CI are confirmed externally after push because a commit cannot
  contain its own identity or subsequent CI result.
- No provider ran. Trading remained disabled; orders constructed/submitted
  remained `0/0`. PR #5 was not modified. PR #6 remains draft and unmerged.
